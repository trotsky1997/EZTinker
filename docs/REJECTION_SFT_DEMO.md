# Rejection SFT Demo: Qwen2-0.5B on GSM8K

这个演示展示了如何使用 **EZTinker** 实现基于拒绝采样的监督微调 (Rejection SFT)，训练 **Qwen2-0.5B-Instruct** 模型在 **GSM8K** 数学推理数据集上。

## 核心特性

- 🎯 **客户端-服务器架构 (CS分离)**
  - Server: EZTinker 负责模型操作（前向后向传播、优化、采样）
  - Client: 实现拒绝采样逻辑算法

- 🧮 **Math-Verify 集成**
  - 使用 huggingface/Math-Verify 库进行数学答案验证
  - 精确评估 GSM8K 问题的答案正确性

- 🎓 **Rank-1 LoRA 训练**
  - 高效的适配器微调
  - 只有 1 个秩的 LoRA 参数需要训练

- 🔄 **Rejection Sampling 工作流**
  1. 对每个问题生成多个候选响应（4-8个）
  2. 使用 Math-Verify 评估每个响应的正确性
  3. 选择最佳响应
  4. 在选定的响应上进行监督微调
  5. 重复多轮并获得准确率提升

## 安装依赖

```bash
cd /path/to/eztinker

# 添加必要的依赖（已包含：datasets, math-verify）
uv add datasets math-verify
```

## 使用说明

### 步骤 1: 启动 EZTinker 服务器

```bash
# Terminal 1
uv run eztinker server
```

服务器启动在 `http://localhost:8000`，可以在浏览器查看 API 文档：`http://localhost:8000/docs`

### 步骤 2: 运行 Rejection SFT Demo

```bash
# Terminal 2
uv run python rejection_sft_demo.py --max-samples 50 --num-candidates 4 --epochs 3
```

**命令行参数说明：**

- `--max-samples N`: 使用的 GSM8K 训练样本数量（默认100）
- `--num-candidates N`: 每个样本生成的候选响应数量（默认4）
- `--epochs N`: 训练轮数（默认3）
- `--learning-rate LR`: 学习率（默认2e-4）
- `--temperature T`: 生成温度（默认0.8）
- `--eval-size N`: 测试集评估样本数量（默认100）
- `--checkpoint`: 每轮保存checkpoint
- `--output-dir DIR`: 输出目录（默认 `data`）

### 完整示例（100样本，3轮训练）：

```bash
uv run python rejection_sft_demo.py \
  --max-samples 100 \
  --num-candidates 8 \
  --epochs 3 \
  --eval-size 200 \
  --checkpoint \
  --temperature 0.7 \
  --learning-rate 3e-4
```

## 工作流程

### Phase 1: 创建训练会话

```python
run_id = create_training_run(
    base_model="Qwen/Qwen2-0.5B-Instruct",
    lora_rank=1  # Rank-1 LoRA
)
```

### Phase 2: 填充拒绝采样缓冲区

对于每个 GSM8K 问题：
1. 生成 4-8 个候选响应
2. 使用 Math-Verify 评估每个响应
3. 选择最佳响应（最高置信度/正确性）
4. 记录到缓冲区
5. 可选：在最佳响应上进行初步训练

### Phase 3: Rejection SFT 训练循环

```
For epoch in epochs:
    For each entry in rejection buffer:
        1. 重新生成候选响应（使用更新后的模型）
        2. 评估并选择最佳
        3. 在该响应上进行梯度更新
        4. 保存 checkpoint（可选）
```

### Phase 4: 评估

在 GSM8K test split 上评估最终模型：
- 生成响应
- 使用 Math-Verify 验证答案
- 计算准确率

## 架构图

```
┌─────────────────────────────────────────────────┐
│   EZTinker Server (FastAPI)                    │
│   - /sample (生成响应)                         │
│   - /v1/runs/{id}/forward_backward (训练)     │
│   - /v1/runs/{id}/optim_step (优化)           │
│   - /v1/runs/{id}/evaluate (评分)             │
│   - /v1/runs/{id}/save (保存)                 │
└─────────────────┬───────────────────────────────┘
                  │ HTTP requests
                  │
┌─────────────────┴───────────────────────────────┐
│   Client (rejection_sft_demo.py)               │
│                                                 │
│   Phase 1: 创建训练会话                       │
│   Phase 2: 填充缓冲区（拒绝采样）              │
│   Phase 3: 训练循环                            │
│   Phase 4: 评估准确率                          │
│                                                 │
│   + Math-Verify (答案验证)                     │
└─────────────────────────────────────────────────┘
```

## 代码结构

### 新文件

1. **`src/eztinker/dataset/gsm8k.py`**
   - GSM8K 数据集加载器
   - Math-Verify 集成
   - 答案提取逻辑

2. **`src/eztinker/rl/rejection_sampler.py`**
   - 拒绝采样逻辑
   - 多候选响应生成
   - 缓冲区和训练

3. **`rejection_sft_demo.py`**
   - 主演示脚本
   - 工作流编排
   - 命令行界面

### 修改的文件

1. **`src/eztinker/models/api.py`**
   - 添加 `EvaluationBatch` 和 `EvaluationRequest` schemas

2. **`src/eztinker/engine/run_manager.py`**
   - 添加 `evaluate_responses()` 方法

3. **`src/eztinker/api/server.py`**
   - 添加 `/v1/runs/{run_id}/evaluate` 端点

## 输出文件

运行后会生成：

```
data/
├── rejection_buffer.jsonl          # 拒绝采样缓冲区
├── rejection_sft_results.json      # 训练结果
└── checkpoints/
    └── {run_id}/
        ├── rejection_sft_epoch_1/
        ├── rejection_sft_epoch_2/
        └── rejection_sft_epoch_3/
```

## 预期结果

在 100 个 GSM8K 训练样本上训练 3 轮后：

| 指标 | 初始 | Epoch 1 | Epoch 2 | Epoch 3 |
|------|------|---------|---------|---------|
| 训练准确率 | ~55% | ~70% | ~78% | ~82% |
| 测试准确率 | ~45% | ~60% | ~68% | ~72% |
| Rejection质量 | 4/8 | 5/8 | 6/8 | 7/8 |

说明：
- **Rejection质量**: 每个示例生成 8 个候选，这里显示被正确选择的准确答案的平均数量
- 这些数字是小样本（100样本）训练的粗略估计，实际性能取决于特定数据和样本

## 故障排查

### 问题 1: 服务器未启动

```
Error: Server not found
```

**解决方案**:
```bash
# 确保服务器在另一个终端运行
uv run eztinker server
```

### 问题 2: Math-Verify 错误

```
Warning: Math-Verify evaluation failed
```

**解决方案**:
- `math-verify` 依赖 `pydantic==2.12.5`，确保版本正确
- 检查网络连接（需要下载评估器模型）

### 问题 3: CUDA out of memory

```
RuntimeError: CUDA out of memory
```

**解决方案**:
```bash
# 使用更小的批次或样本数量
python rejection_sft_demo.py --max-samples 25 --num-candidates 2
```

### 问题 4: 准确率没有提升

**检查项**:
1. 确认训练循环正确执行（查看服务器的响应）
2. 检查 LoRA 配置（rank 应该大于0）
3. 调整学习率（`--learning-rate 3e-4` 或 `5e-4`）
4. 增加训练样本数量（`--max-samples 500`）
5. 增加候选响应数量（`--num-candidates 8`）

## 高级用法

### 自定义模型

```python
# 修改 rejection_sft_demo.py 中的 MODEL_ID
MODEL_ID = "Qwen/Qwen2-7B-Instruct"
# 然后运行
python rejection_sft_demo.py --max-samples 50
```

### 调整 LoRA 参数

```python
# 在 rejection_sft_demo.py 的 create_training_run() 中修改
run_id = create_training_run(
    base_model=MODEL_ID,
    lora_rank=4,  # 更高的秩
    lora_alpha=8,  # 不同的alpha
    lora_dropout=0.1,  # 更高的dropout
)
```

### 批量训练

服务器自动对积累的批次进行梯度累积。客户端可以通过多次调用 `forward_backward` 积累梯度。

```python
# 多次forward-backward，一次optim-step
for i in range(4):
    fb_response = requests.post(...)
    wait_for_job(fb_response.json()["job_id"])
# 然后optim_step
optim_response = requests.post(...)
```

## 数学验证详情

**Math-Verify** 是一个强大的数学答案验证库，专门设计用于：

1. **答案提取**: 从自由文本中提取数值答案
2. **等价性检查**: 验证不同的数学表达式是否等价
3. **单位转换**: 处理不同的单位表示

在我们的实现中，使用 `is_equal()` 和 `is_equiv()` 方法来比较答案：

```python
# 答案格式示例
question = "Janet's ducks lay eggs..."
model_response = "She has 21 ducks. The answer is 21"
ground_truth = "21"

# Math-Verify 会
1. 提取预测和真实答案中的数字
2. 进行数值比较
3. 检查不同格式（"21" vs "twenty one" vs "21.0"）
4. 返回置信度分数
```

## EZTinker vs 传统训练

| 特性 | EZTinker (Client-Server) | 传统 SFT |
|------|--------------------------|----------|
| **架构** | ✅ 分离式（客户端算法，服务器模型操作） | ❌ 一体化 |
| **LoRA** | ✅ 自动应用到base model | ❌ 需要手动配置 |
| **Rank-1 训练** | ✅ 默认支持 | ⚠️ 需要复杂配置 |
| **Rejection采样** | ✅ 客户端灵活实现 | ❌ 需要大量修改 |
| **Math-Verify集成** | ✅ 独立于服务器 | ⚠️ 需要深度集成 |
| **扩容性** | ✅ 可扩展到多GPU服务器 | ❌ 单机训练 |
| **灵活性** | ✅ 无需重启服务器即可修改算法 | ❌ 需要重新编译/关闭 |

## 下一步优化

### 短期（1-2天）
- [ ] 添加并行化生成（batch sampling）
- [ ] 改进答案提取逻辑
- [ ] 添加奖励模型作为评分器
- [ ] 实现DPO/Optimal算法

### 中期（1周）
- [ ] 支持多GPU并行训练
- [ ] 添加监控dashboard
- [ ] 实现实时buffer更新
- [ ] 集成更多数学数据集（MATH, AQuA等）

### 长期（未来）
- [ ] 端到端RLHF管道
- [ ] 分布式训练支持
- [ ] OpenAI-compatible API
- [ ] Web UI控制台

## 理论背景: Rejection SFT

### 为什么使用 Rejection Sampling？

传统监督微调 (SFT) 简单地训练模型模仿提供的示范。**Rejection SFT** 改进这一过程：

1. **多样性生成**: 生成多个响应而不是单一响应
2. **选择最佳**: 使用某种评分机制选择"最佳"响应
3. **训练增强**: 在高质量的选定响应上进行训练

### 数学公式

对于给定的对话 `(x, y)`:

```python
# 传统 SFT
L_SFT = -log P_θ(y | x)

# Rejection SFT
candidates = [y_1, y_2, ..., y_N]  ~  P_θ(y | x)
scores = [s_1, s_2, ..., s_N]  ←  f(y_i, ground_truth)
y_best = argmax(scores)  # 选择最佳
L_RSFT = -log P_θ(y_best | x)  # 只在最佳上训练
```

其中 `f` 是我们的评分函数（Math-Verify）。

### Rejection 的好处

1. **隐式探索**: 模型探索不同的生成策略
2. **高质量训练**: 只训练在"正确"或"接近正确"的响应上
3. **自我提升**: 逐步改进生成质量
4. **灵活评分**: 可以轻松更换评分策略（exact match, LLM judge, reward model）

### 与 RLHF 的关系

Rejection SFT 可以看作 RLHF 的简化形式：
- **RLHF**: 需要训练完整的奖励模型和策略梯度更新
- **Rejection SFT**: 只选择最佳响应，监督训练

对于小模型（0.5B）和小任务（GSM8K），Rejection SFT 通常足够且更稳定。

## 引用

如果使用此代码，请引用：

```bibtex
@software{eztinker_rejection_sft,
  title = {EZTinker: Rejection SFT Demo with GSM8K and Qwen2-0.5B},
  author = {EZTinker Research Group},
  year = {2025},
  url = {https://github.com/yourusername/eztinker},
}
```

相关库：
- **Math-Verify**: [huggingface/Math-Verify](https://huggingface.co/datasets/huggingface/Math-Verify)
- **EZTinker**: 本项目
- **GSM8K**: [OpenAI GSM8K](https://github.com/openai/grade-school-math)
- **Qwen**: [QwenLM](https://github.com/QwenLM/Qwen2)

## 许可

MIT License（与 EZTinker 主项目相同）

## 联系方式

- GitHub Issues: [报告问题或请求功能](https://github.com/yourusername/eztinker/issues)
- Discussions: [提问和讨论](https://github.com/yourusername/eztinker/discussions)